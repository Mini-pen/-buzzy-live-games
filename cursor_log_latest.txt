# Journal de bord — buzzy-live-games (session la plus récente)

**Date** : 2026-05-14 (soir)  
**Contexte** : Correction du scénario « guess by color » : ce n’était pas une devinette de couleur ni un QCM.

## Objectifs

- Afficher **une image à la fois** et attendre un **buzz + réponse orale** (comme les manches à questions libres).

## Réalisations

- Type de manche pack **`image_buzz`** : tableau `slides` (`imageUrl`, `prompt` optionnel pour une consigne courte).
- Snapshot `gameBoard.kind === "image_buzz"` ; bouton animateur **« Image suivante → »** (même logique d’enchaînement que le blind audio).
- Pack `games/guess_by_color/quiz_with_images.json` réécrit (titre « Série d’images (réponse orale) », pastilles + exemple HTTPS).
- UI : `GameBoardPanel` + joueur + texte d’aide scénario (packs JSON / séries d’images sans QCM).

## Prochain focus suggéré

- Si besoin : enrichir la diffusion / grand écran pour mettre encore plus en avant l’image (même rendu que le QCM mais sans liste A/B/C/D).

## Références

- Commit `4c248fa` ; fichiers `webserver/src/games/pack.ts`, `partySnapshotPresenter.ts`, `store.ts`, `client/App.tsx`, pack JSON.
